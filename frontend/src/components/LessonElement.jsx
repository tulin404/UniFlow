export default function LessonElement({ actId, actName, actLink, dueDate }) {
    return (
        <article className="bg-card hover:bg-card-hover font-[Inter] transition-all duration-200 rounded-md border-card-border border-2">
            <p className="text-text">Heyyyyy</p>
        </article>
    );
};